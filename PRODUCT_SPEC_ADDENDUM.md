# Product Manager 追加仕様（反映済み）

1. **ドライランモード**
   - `--dry-run` で翻訳対象テキスト件数のみ表示し、保存しない。
2. **スライド範囲指定**
   - `--slide-range 1-5,8` で翻訳対象スライドを限定。
3. **失敗時フォールバック**
   - Ollama翻訳失敗時は原文維持し、処理継続（最後に件数サマリ）。
4. **ログ可視化**
   - `--verbose` で shape 単位の処理ログを出力。
5. **安全性**
   - 入力拡張子が `.pptx` 以外は即エラー。

> 今回の初期実装では 3,5 を実装。1,2,4 は次スプリント候補として backlog 登録。
